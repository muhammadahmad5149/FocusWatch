{
  "name": "screenguardian",
  "version": "1.0.0",
  "description": "Windows parental control screenshot monitor with Electron admin dashboard.",
  "main": "main/bootstrap.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "service": "node service/index.js",
    "install-service": "node scripts/install-service.js",
    "uninstall-service": "node scripts/uninstall-service.js",
    "check": "node --check main/bootstrap.js && node --check main/main.js && node --check main/preload.js && node --check service/index.js && node --check service/scheduler.js && node --check service/screenshotEngine.js && node --check service/sentStore.js && node --check email/mailer.js && node --check utils/auth.js && node --check utils/env.js && node --check utils/fileSystem.js && node --check utils/internet.js && node --check utils/logger.js && node --check utils/paths.js && node --check utils/permissions.js && node --check utils/serviceInstaller.js && node --check utils/serviceManager.js && node --check utils/settings.js && node --check ui/renderer.js && node --check scripts/install-service.js && node --check scripts/uninstall-service.js",
    "dist": "electron-builder",
    "dist:win": "electron-builder --win nsis"
  },
  "build": {
    "appId": "com.screenguardian.desktop",
    "productName": "ScreenGuardian",
    "asar": false,
    "files": [
      "main/**/*",
      "service/**/*",
      "email/**/*",
      "utils/**/*",
      "ui/**/*",
      "scripts/**/*",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "requestedExecutionLevel": "asInvoker"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": true,
      "allowElevation": true,
      "allowToChangeInstallationDirectory": true
    }
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "node-windows": "^1.0.0-beta.8",
    "nodemailer": "^8.0.7",
    "screenshot-desktop": "^1.15.4"
  },
  "devDependencies": {
    "electron": "^42.0.0",
    "electron-builder": "^26.8.1"
  }
}
